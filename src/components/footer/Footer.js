// Importing node modules
import React from 'react'

// Importing local project files
import './footer.scss';

const Footer = () => {
    return (
        <div>Footer</div>
    )
}

export default Footer